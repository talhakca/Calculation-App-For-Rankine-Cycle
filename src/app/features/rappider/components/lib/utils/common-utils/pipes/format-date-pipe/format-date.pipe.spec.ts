import { FormatDatePipe } from './format-date.pipe';

describe('Pipe: FormatDatee', () => {
  it('create an instance', () => {
    const pipe = new FormatDatePipe();
    expect(pipe).toBeTruthy();
  });
});
